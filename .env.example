# Port for the service (Railway sets this automatically)
PORT=3000

# Optional API key for authentication
# API_KEY=your-secret-api-key

# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://your-n8n-instance.n8n.cloud,https://your-app.com

# Puppeteer executable path (set automatically in Docker)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium